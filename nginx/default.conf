server {
    listen 80;
    server_name _;

    # ✅ อนุญาตอัปโหลดไฟล์ใหญ่ (เช่น 200MB)
    client_max_body_size 200m;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass         http://backend:8000/api/;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto https;

        # (เสริมถ้าไฟล์ใหญ่มาก/อัปโหลดช้า)
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        client_body_timeout 600s;
        # หากต้องการ disable buffering (เช่น อัปโหลดสตรีม)
        # proxy_request_buffering off;
    }

    # 🛡️ Proxy admin Django
    location /admin/ {
        proxy_pass         http://backend:8000/admin/;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto https;
    }

    # 🧊 Static files (Django collectstatic)
    location /static/ {
        alias /static/;
        access_log off;
        expires 7d;
    }

    # 🖼️ Media files (upload)
    location /media/ {
        alias /media/;
        access_log off;
        expires 7d;
    }

    # 🪄 SPA fallback — redirect ทุก request ที่ไม่ match ไป index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}